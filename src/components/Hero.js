import React from 'react';
import Container from 'react-bootstrap/Container';

const HeroInicio = () => {
    return (
        <Container id="hero">
            <h1>Hero</h1>
        </Container>
    );
};

export default HeroInicio;