import React from 'react';
import Section from '../ui/Section';

const FAQ = () => {
    return (
        <Section 
            id="faq"
            title="FAQ" 
            description="Preguntas frecuentes"
            bgColor="var(--helio-bg-light)"
        >
            {/* Contenido de FAQ aquí */}
        </Section>
    );
};

export default FAQ; 