import React from 'react';
import Container from 'react-bootstrap/Container';

const Planes = () => {
    return (
        <Container id="planes">
            <h1>Planes</h1>
        </Container>
    );
};

export default Planes;